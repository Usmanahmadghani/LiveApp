import { UsdInrPipesPipe } from './usd-inr-pipes.pipe';

describe('UsdInrPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new UsdInrPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
